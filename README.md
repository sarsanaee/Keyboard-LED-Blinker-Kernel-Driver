# Keyboard-LED-Blinker-Kernel-Driver
A kernel Driver that get user input and blinks keyboard leds with binary representation for those characters

### For better undrestanding 

Just look at `connector` and `script`
